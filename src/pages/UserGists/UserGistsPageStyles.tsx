import styled from 'styled-components';
import { font } from './../../utils/theme';

export const StyledUserGiststPageContainer = styled.div`
  display: flex;
  flex-direction: column;
  font: ${font.primary};
  margin: 48px;
`;